name: Env Demo

on:
  push:

jobs:
  env:
    runs-on: ubuntu-latest

    env:
      DEPLOY_ENV: staging

    steps:
      - name: Exibir variável
        run: |
          echo "Ambiente: $DEPLOY_ENV"